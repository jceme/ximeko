# Application dependencies

require:
    - play
    - provided -> scribe 1.3.7
    - provided -> gson 2.3.1-javadoc

repositories:
  - provided:
      type:       local
      artifact:   "${application.path}/jars/[module]-[revision].jar"
      contains:
        - provided -> *