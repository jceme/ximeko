# Application dependencies

require:
    - play
    - provided -> scribe 1.3.7

repositories:
  - provided:
      type:       local
      artifact:   "${application.path}/jars/[module]-[revision].jar"
      contains:
        - provided -> *